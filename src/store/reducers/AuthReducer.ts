import React from "react";

let initialState = {
    isAuth:false,
};

const AuthReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default AuthReducer;